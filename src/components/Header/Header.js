import React from 'react';

const Header = () => {

    const style = {
        color: "#006768"
    }
    return (
        <header style={style}>
            <h1>Product Card Project</h1>
        </header>
    )
}

export default Header;